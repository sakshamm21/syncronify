# Backend API base URL (must be reachable from the browser)
NEXT_PUBLIC_BASE_URL=http://localhost:4000

# Mapbox access token (optional — required for the map/navigation feature)
NEXT_PUBLIC_MAPBOX_TOKEN=
